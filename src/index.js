console.log('hi')

//Build a node class/factory
 
const createNode = (value) => {
    const node = {
        value: null,
        next: null
    }
    return node;
}

const tree = (array) => {
    //should have a root attribute - that uses the return value of buildtree()

    const buildTree = () => {
        // Takes array of data and returns balanced BST
        // Should return the level 0 node

    }

    
}